name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

env:
  UV_EXTRA: dev

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v1
        with:
          version: "0.7.12" # Using a recent stable version

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run tests
        run: uv run --extra dev pytest

      - name: Build package
        run: |
          uv pip install build
          uv run python -m build

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: dist/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to PyPI
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
        run: |
          if [ -z "${TWINE_PASSWORD}" ]; then
            echo "⚠️ PYPI_TOKEN secret is missing. Skipping upload."
            exit 0
          fi

          echo "Publishing to PyPI..."
          # Check current package version
          PACKAGE_VERSION=$(python -c "from openfatture import __version__; print(__version__)")
          echo "Package version: $PACKAGE_VERSION"

          # Try to upload with skip-existing flag to avoid errors on duplicate uploads
          uvx twine upload --skip-existing dist/* --verbose

  # Docker Hub publishing - to be configured later
  # docker:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     - name: Extract version from tag
  #       id: version
  #       run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

  #     - name: Build and push
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           gianlucamazza/openfatture:latest
  #           gianlucamazza/openfatture:${{ steps.version.outputs.VERSION }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
